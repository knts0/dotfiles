[time]
disabled=false
format = '[\[ $time \]]($style) '

[directory]
truncation_length = 10 # ディレクトリ表示の最大文字数
truncate_to_repo = false

[aws]
disabled=true

[gcloud]
format = 'on [$symbol$account(@$domain)(\($project\))]($style) '

[git_status]
# デフォルト設定を適用
